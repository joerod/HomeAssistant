# Turns off pool filter
alias: 'Pool Filter Off 5:45PM'
trigger:
  platform: time
  at: "17:45:00"
condition:
  - condition: state
    entity_id: switch.pool_filter
    state: 'on'        
action:
  service: switch.turn_off
  entity_id: switch.pool_filter  
