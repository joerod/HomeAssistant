# Turns off pool filter
alias: 'Pool Filter Off 7:45AM'
trigger:
  platform: time
  at: "7:45:00"
condition:
  - condition: state
    entity_id: switch.pool_filter
    state: 'on'        
action:
  service: switch.turn_off
  entity_id: switch.pool_filter  
